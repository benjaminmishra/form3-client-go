package f3client_test

// const (
// 	// baseURLPath is a non-empty Client.BaseURL path to use during tests,
// 	// to ensure relative URLs are used for all endpoints.
// 	baseURLPath = "/v1"
// )

// func setup() (client *f3client.Client, mux *httptest.Server, serverURL string, teardown func()) {

// 	return client, mux, mux.URL, mux.Close
// }
